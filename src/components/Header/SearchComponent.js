import React, { Component } from "react";
import Header from "./components/Header/Header";

const SearchComponent=({data})=> {

  return (
    <SearchBar
    noIcon
    containerStyle={{backgroundColor:'fff'}};
    inputSyle={{backgroundColor:'white'}};
    lightTheme
  )
}